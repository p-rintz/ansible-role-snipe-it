---
- hosts: localhost
  connection: local
  remote_user: root
  roles:
    - ansible-role-snipe-it

  tasks:
  - name: Test reachability of /setup
    ansible.builtin.uri:
      url: "http://localhost/setup"
      method: GET
    register: _result
